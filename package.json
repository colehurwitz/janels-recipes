{
  "name": "janels-recipes",
  "version": "1.0.0",
  "description": "Janel's Recipes — a warm personal cookbook built with Eleventy + Pagefind, deployed on GitHub Pages.",
  "type": "module",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "search": "pagefind --site _site",
    "build:full": "eleventy && pagefind --site _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pagefind": "^1.1.0"
  }
}
